# Moryne 使用说明

一个日历 App：每天可以跟 AI 对话留言，也可以设定时推送让它主动找你。

---

## 一、装好之后第一件事：填 API key

**不填 key，AI 功能一个都用不了**（日历只能当纯本地记事本）。

Moryne 不自带任何 AI 服务，用的是你自己的账号和额度。支持 OpenAI、
Google Gemini、Anthropic，以及任何「OpenAI 兼容」的第三方接口。

1. 打开 App，左上角滑出侧边栏 → **设置**
2. 找到 **供应商**（Provider）→ 选一个，或者新建一个
3. 填 **API Key**；如果用第三方中转，还要改 **接口地址**（Base URL）
4. 回到聊天页，顶部会有模型选择，挑一个模型

填完先在聊天页发一句话试试。**能正常回复，再往下配日历和推送**——
不然后面出问题分不清是 key 的事还是设置的事。

> 你的 key 只存在这台手机上，不会上传到任何地方。App 里也没有任何数据统计。

---

## 二、日历怎么用

侧边栏 → **日历**。

页面上半部分是月历，下半部分是**当天的对话**。

### 每天写点什么

- 在下方输入框打字发送，就记在今天名下
- 如果开了「AI 回复」，发完 AI 会针对你写的内容回一段话
- 想换个话题、不想让 AI 继续接着前面聊：点 **新话题**，
  之后的对话不再带上前面的内容（但旧内容还看得见）
- 某条 AI 回复不满意，可以让它 **重新生成**

### 标注重要的日子

点月历上任意一个日期 → 弹出面板 → 加标注（标题 + 备注）。
标注过的日期在月历上会有个小圆点。

### 纪念日 / 倒计时

页面顶部有两个标签页：**日历** 和 **纪念日**。

你加过的标注会出现在「纪念日」里，自动算天数。每条可以单独切换显示方式：

- **正数日**：从那天到今天过了多少天（适合生日、开始做某件事的日子）
- **倒计时**：距离下一次还有多少天（适合每年一次的日子）

点那一条就能切。

> 「纪念日」里是空的，就是你还没加过标注。App 不预置任何日期。

### 设置 AI 怎么说话

日历页右上角 → **对话**：

- **留言时让 AI 回复** —— 关掉的话日历就是个纯本地记事本，完全不联网
- **模型** —— 可以跟聊天页用不一样的模型（想省钱就在这挑个便宜的）
- **提示词** —— 决定 AI 的身份和语气，整段替换成你想要的

默认提示词只是个能用的起点，建议自己改。

### 导出备份

日历页右上角的导出按钮，可以导出成 **JSON**（完整数据）或
**Markdown**（给人看的）。

> ⚠️ **建议定期导出。** 目前只能导出、不能导入，
> 但至少卸载或换手机之前导一份，内容不会丢。

---

## 三、定时推送

日历页右上角 → **推送**。

打开总开关的时候，会让你授权两个权限，**都必须给，不给推送不会响**：

1. **通知权限** —— 不给的话消息生成了也不会弹出来
2. **精确闹钟权限** —— 这个要跳到系统设置里开，不给的话时间会不准或者根本不触发

### 还有一步系统设置要做（重要）

国产安卓 ROM（小米、华为、OPPO、vivo、荣耀等）会为了省电把后台 App 冻结，
**这是定时推送不响最常见的原因**，跟 App 本身没关系。

请去系统设置里做这两件事：

- 把 Moryne 的 **电池优化 / 省电策略** 设成「不限制」或「允许后台活动」
- 在最近任务列表里给 Moryne **加锁**（不同 ROM 叫法不同：锁定、加锁、上锁）

做完这两步，推送才稳。

### 推送时间

可以加多个时间点，每天都在这些时间推。点已有的时间可以改，也可以删。

### 两种推送内容（这是核心选择）

**方式一：每次让模型生成**

每次推送都调 AI 现写一条，内容每次都不一样，会读当天日历里已有的内容。
**会产生 API 费用**（一次推送约等于一次简短对话）。

底下的「推送提示词」决定它以什么身份、什么语气来找你。

**方式二：用我写好的固定文案**

自己写好几条话，到点从里面挑一条发。**完全不联网，不花一分钱。**

- 点「+ 添加一条文案」，想写几条写几条
- 挑选方式：
  - **随机挑一条** —— 每次随机
  - **按顺序轮流** —— 第一次发第 1 条，第二次发第 2 条，到底了回到第 1 条

适合「每天提醒我喝水」这种不需要 AI 现编的场景。

> 选了固定文案但列表是空的，到点会收到一条提醒你去填文案的通知，
> 不会静默地什么都不发。

### 通知里显示什么

- **占位符** —— 通知栏只显示你自定的一句话（比如「来看看」），
  不透露具体内容，要点进来才看得到。适合不想在锁屏上被别人看见的情况
- **消息内容** —— 通知栏直接显示消息开头

「通知标题」也可以自己改。

### 推送来了之后

消息会存进当天的日历里，点通知就跳到日历页。

**如果某次没收到内容，但收到了一条「推送生成失败」的通知**——
那是 AI 调用出问题了（网络断了、key 余额不够、模型返回空等），
通知上会写原因。这是故意做的：失败也让你知道，而不是悄无声息。

---

## 四、出问题先看这里

**推送完全不响**
1. 通知权限和精确闹钟权限都给了吗
2. 电池优化设成不限制了吗、最近任务里加锁了吗（**最常见的原因**）
3. 推送总开关开了吗，时间设对了吗
4. 改完设置**记得点保存**，闹钟是保存的时候才排的

**推送响了但没内容**
看有没有一条「推送生成失败」通知，上面写了原因。
最常见是 key 余额不够或者网络不通。

**AI 不回复 / 报错**
先回聊天页发一句话试试。聊天页也不行 → key 或接口地址的问题。
聊天页正常但日历不回 → 检查日历「对话」设置里的「留言时让 AI 回复」有没有开。

**想省钱**
- 推送用「固定文案」模式，完全不花钱
- 日历「对话」设置里挑个便宜的模型
- 不想让 AI 回复就关掉「留言时让 AI 回复」，日历照样能记事

**换手机 / 要卸载重装**
先去日历页导出一份 JSON。目前没有导入功能，但数据至少留在手上了。

---

## 五、几件要知道的事

- 数据全部存在这台手机上，不联网同步，也没有账号系统
- **卸载 App 会清掉所有日历数据**，重要内容记得先导出
- API key 明文存在手机本地。手机借人、送修之前自己掂量一下
- 每一次 AI 回复和每一条 AI 生成的推送都会消耗你自己的额度

---

Moryne 基于开源项目 [RikkaHub](https://github.com/rikkahub/rikkahub) 修改而来。
